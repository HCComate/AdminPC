# AdminPC 환경변수 설정
# 이 파일을 복사해서 .env 파일로 사용하세요
# cp .env.example .env

# ── AdminPC-Server 주소 ───────────────────────
# 같은 PC에서 실행 시: http://localhost:5000
# 다른 PC에서 실행 시: http://<PC_LAN_IP>:5000
VITE_SERVER_URL=http://localhost:5000
